#!/bin/sh
# $1 pto
# $2.. targets

PTO=$1
PREFIX=`basename $1 .pto`
if [ -z "$MAKE" ] ; then
	export MAKE=make
fi

shift

rm -f "$@"

pto2mk -o "$PTO.mk" -p "$PREFIX" "$PTO"
$MAKE -e -f "$PTO.mk" all
$MAKE -e -f "$PTO.mk" clean

touch "$@"
